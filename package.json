{
  "name": "kids-memory-games",
  "version": "1.0.0",
  "description": "Memory games platform for kids — Astro + Vanilla TS + Cloudflare Pages",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest",
    "test:contract": "vitest run src/tests/contract",
    "test:unit": "vitest run src/tests/unit",
    "check-asset-budget": "tsx src/scripts/check-asset-budget.ts",
    "export:scorm": "tsx src/scripts/export-scorm.ts",
    "lint": "eslint src --ext .ts,.astro"
  },
  "dependencies": {
    "astro": "^4.14.0"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "@types/adm-zip": "^0.5.8",
    "@types/node": "^20.0.0",
    "adm-zip": "^0.6.0",
    "tailwindcss": "^3.4.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
